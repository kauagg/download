(function (root, factory) {
  if (typeof define === 'function' && define.amd)
    define('app', ['exports', 'kotlin'], factory);
  else if (typeof exports === 'object')
    factory(module.exports, require('kotlin'));
  else {
    if (typeof kotlin === 'undefined') {
      throw new Error("Error loading module 'app'. Its dependency 'kotlin' was not found. Please, check whether 'kotlin' is loaded prior to 'app'.");
    }
    root.app = factory(typeof app === 'undefined' ? {} : app, kotlin);
  }
}(this, function (_, Kotlin) {
  'use strict';
  var println = Kotlin.kotlin.io.println_s8jyv4$;
  function main(args) {
    println('Hello, World!');
  }
  _.main_kand9s$ = main;
  Kotlin.defineModule('app', _);
  main([]);
  return _;
}));

//@ sourceMappingURL=app.js.map
